import {useState} from 'react';

interface PokemonCardProps{
    pokemon:{
    name: string;
    };
}

export function Slider(props: PokemonCardProps){
   
   // const [card, setCard] = useState<pokemon[]>([]);
    return(
        <>

        </>
    );
}